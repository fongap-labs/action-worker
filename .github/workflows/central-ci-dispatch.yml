name: Handle Central CI Dispatch

on:
  repository_dispatch:
    types: [run-central-ci, run-central-ci-ref]

permissions:
  contents: read

concurrency:
  group: central-ci-${{ github.event.client_payload.repository }}-${{ github.event.client_payload.pr_number || github.event.client_payload.head_sha }}
  cancel-in-progress: ${{ github.event.action == 'run-central-ci' }}

jobs:
  prepare:
    name: Prepare Central CI
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    outputs:
      repository: ${{ steps.payload.outputs.repository || steps.ref_facts.outputs.repository }}
      pr_number: ${{ steps.payload.outputs.pr_number || steps.ref_facts.outputs.pr_number }}
      base_sha: ${{ steps.facts.outputs.base_sha || steps.ref_facts.outputs.base_sha }}
      head_sha: ${{ steps.facts.outputs.head_sha || steps.ref_facts.outputs.head_sha }}
      control_ref: ${{ steps.ci_control.outputs.control_ref || steps.ref_facts.outputs.control_ref || 'main' }}
      candidate_control: ${{ steps.ci_control.outputs.candidate_control || 'false' }}
      is_private: ${{ steps.visibility.outputs.is_private }}
      has_windows: ${{ steps.capabilities.outputs.has_windows }}
      should_run: ${{ steps.ref_facts.outputs.should_run || 'true' }}

    steps:
      - name: Checkout control plane
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24

      - name: Validate PR request
        id: payload
        if: github.event.action == 'run-central-ci'
        env:
          PAYLOAD: ${{ toJSON(github.event.client_payload) }}
          AW_REPOSITORY_POLICY: ${{ vars.AW_REPOSITORY_POLICY }}
        run: node scripts/validate-pr-payload.ts "$PAYLOAD"

      - name: Resolve branch CI request
        id: ref_facts
        if: github.event.action == 'run-central-ci-ref'
        env:
          GH_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ github.event.client_payload.repository }}
          REQUESTED_HEAD_SHA: ${{ github.event.client_payload.head_sha }}
          SCHEMA_VERSION: ${{ github.event.client_payload.schema_version }}
          REQUEST_ID: ${{ github.event.client_payload.request_id }}
        shell: bash
        run: |
          set -Eeuo pipefail

          [ "$SCHEMA_VERSION" = "1" ] || {
            echo "::error::Unsupported schema_version: $SCHEMA_VERSION" >&2
            exit 64
          }
          [ -n "$REQUEST_ID" ] || {
            echo "::error::request_id is required." >&2
            exit 64
          }
          [[ "$REQUESTED_HEAD_SHA" =~ ^[0-9a-f]{40}$ ]] || {
            echo "::error::head_sha must be a full commit SHA." >&2
            exit 64
          }

          default_branch="$(gh api "repos/$REPOSITORY" --jq '.default_branch')"
          default_sha="$(gh api "repos/$REPOSITORY/commits/$default_branch" --jq '.sha')"
          [ "$REQUESTED_HEAD_SHA" = "$default_sha" ] || {
            echo "::error::Central CI request is stale; default branch has moved." >&2
            exit 75
          }

          base_sha="$(gh api "repos/$REPOSITORY/commits/$REQUESTED_HEAD_SHA" --jq '.parents[0].sha // .sha')"
          evidence_state="$(
            gh api "repos/$REPOSITORY/commits/$REQUESTED_HEAD_SHA/status" \
              --jq '[.statuses[] | select(.context == "CI Evidence")][0].state // ""'
          )"
          should_run=true
          if [ "$evidence_state" = "success" ]; then
            should_run=false
          fi
          {
            echo "repository=$REPOSITORY"
            echo "pr_number=0"
            echo "base_sha=$base_sha"
            echo "head_sha=$REQUESTED_HEAD_SHA"
            echo "control_ref=$REQUESTED_HEAD_SHA"
            echo "should_run=$should_run"
          } >> "$GITHUB_OUTPUT"

      - name: Validate central CI target
        env:
          AW_REPOSITORY_POLICY: ${{ vars.AW_REPOSITORY_POLICY }}
          REPOSITORY: ${{ steps.payload.outputs.repository || steps.ref_facts.outputs.repository }}
        run: node scripts/repository-policy.ts validate "$REPOSITORY" pr

      - name: Resolve target visibility
        id: visibility
        env:
          GH_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ steps.payload.outputs.repository || steps.ref_facts.outputs.repository }}
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "is_private=$(gh api "repos/$REPOSITORY" --jq '.private')" >> "$GITHUB_OUTPUT"

      - name: Validate control credential
        if: github.event.action == 'run-central-ci'
        env:
          GH_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ steps.payload.outputs.repository }}
          PR_NUMBER: ${{ steps.payload.outputs.pr_number }}
        run: node scripts/validate-control-access.ts "$REPOSITORY" "$PR_NUMBER"

      - name: Resolve PR CI control
        id: ci_control
        if: github.event.action == 'run-central-ci'
        env:
          AW_CONTROL_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ steps.payload.outputs.repository }}
          PR_NUMBER: ${{ steps.payload.outputs.pr_number }}
          EXPECTED_HEAD_SHA: ${{ github.event.client_payload.head_sha }}
        run: node scripts/resolve-ci-control-ref.ts "$REPOSITORY" "$PR_NUMBER" "$EXPECTED_HEAD_SHA"

      - name: Resolve CI capabilities
        id: capabilities
        env:
          AW_CONTROL_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ steps.payload.outputs.repository || steps.ref_facts.outputs.repository }}
          CONTROL_REF: ${{ steps.ci_control.outputs.control_ref || steps.ref_facts.outputs.control_ref || 'main' }}
        run: node scripts/resolve-ci-capabilities.ts "$REPOSITORY" "$CONTROL_REF"

      - name: Resolve PR facts
        id: facts
        if: github.event.action == 'run-central-ci'
        env:
          GH_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ steps.payload.outputs.repository }}
          PR_NUMBER: ${{ steps.payload.outputs.pr_number }}
        run: node scripts/resolve-pr-facts.ts fetch

      - name: Validate requested revision
        if: github.event.action == 'run-central-ci'
        env:
          REQUESTED_HEAD_SHA: ${{ github.event.client_payload.head_sha }}
          RESOLVED_HEAD_SHA: ${{ steps.facts.outputs.head_sha }}
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$REQUESTED_HEAD_SHA" =~ ^[0-9a-f]{40}$ ]]
          if [ "$REQUESTED_HEAD_SHA" != "$RESOLVED_HEAD_SHA" ]; then
            echo "::error::Central CI request is stale; PR head changed after dispatch." >&2
            exit 75
          fi

  linux:
    name: Central CI / Linux
    needs: prepare
    if: needs.prepare.outputs.should_run != 'false'
    runs-on: ubuntu-24.04
    timeout-minutes: 180

    steps:
      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22

      - name: Checkout target PR
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ needs.prepare.outputs.repository }}
          ref: ${{ needs.prepare.outputs.head_sha }}
          token: ${{ secrets.AW_CONTROL_TOKEN }}
          path: target
          fetch-depth: 0
          persist-credentials: false

      - name: Checkout trusted CI control
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ needs.prepare.outputs.repository }}
          ref: ${{ needs.prepare.outputs.control_ref }}
          token: ${{ secrets.AW_CONTROL_TOKEN }}
          path: ci-control
          fetch-depth: 1
          persist-credentials: false

      - name: Run trusted Linux CI
        env:
          CENTRAL_CI_BASE_SHA: ${{ needs.prepare.outputs.base_sha }}
          CENTRAL_CI_HEAD_SHA: ${{ needs.prepare.outputs.head_sha }}
          CENTRAL_CI_PR_NUMBER: ${{ needs.prepare.outputs.pr_number }}
          TARGET_REPOSITORY: ${{ needs.prepare.outputs.repository }}
          TARGET_PRIVATE: ${{ needs.prepare.outputs.is_private }}
        shell: bash
        run: |
          set -Eeuo pipefail
          script="$GITHUB_WORKSPACE/ci-control/.github/scripts/central-ci.sh"
          test -f "$script"

          log="$RUNNER_TEMP/central-ci-linux.log"
          failure_meta="$RUNNER_TEMP/central-ci-linux.failure"
          trace_env="$RUNNER_TEMP/central-ci-bash-env"
          cat >"$trace_env" <<'EOF'
          trap 'code=$?; if [ -n "${CENTRAL_CI_FAILURE_FILE:-}" ]; then printf "%s:%s\n" "$LINENO" "$code" > "$CENTRAL_CI_FAILURE_FILE"; fi' ERR
          EOF

          set +e
          CENTRAL_CI_FAILURE_FILE="$failure_meta" BASH_ENV="$trace_env" \
            bash "$script" "$GITHUB_WORKSPACE/target" >"$log" 2>&1
          code=$?
          set -e

          if [ "$code" -ne 0 ]; then
            if [ "$TARGET_PRIVATE" = "false" ]; then
              cat "$log"
            else
              diagnostic=""
              if [ -f "$failure_meta" ]; then
                IFS=: read -r failure_line failure_code <"$failure_meta" || true
                if [[ "$failure_line" =~ ^[0-9]+$ ]] && [[ "$failure_code" =~ ^[0-9]+$ ]]; then
                  diagnostic=" Trusted CI failed at line $failure_line with exit code $failure_code."
                fi
              fi
              echo "::error::Central Linux CI failed for $TARGET_REPOSITORY.$diagnostic Detailed output is suppressed because the target repository is private."
            fi
            exit "$code"
          fi

          echo "Central Linux CI passed for $TARGET_REPOSITORY."

  security:
    name: Central CI / Security
    needs: prepare
    if: needs.prepare.outputs.should_run != 'false'
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout control plane
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24

      - name: Checkout target source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ needs.prepare.outputs.repository }}
          ref: ${{ needs.prepare.outputs.head_sha }}
          token: ${{ secrets.AW_CONTROL_TOKEN }}
          path: target
          fetch-depth: 0
          persist-credentials: false

      - name: Validate security gate
        run: node scripts/validate-security.ts "${{ needs.prepare.outputs.base_sha }}" "${{ needs.prepare.outputs.head_sha }}" "$GITHUB_WORKSPACE/target" "$GITHUB_WORKSPACE/policies/security.json"

  windows:
    name: Central CI / Windows
    needs: prepare
    if: needs.prepare.outputs.should_run != 'false' && needs.prepare.outputs.has_windows == 'true'
    runs-on: windows-latest
    timeout-minutes: 90

    steps:
      - name: Checkout target PR
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ needs.prepare.outputs.repository }}
          ref: ${{ needs.prepare.outputs.head_sha }}
          token: ${{ secrets.AW_CONTROL_TOKEN }}
          path: target
          fetch-depth: 0
          persist-credentials: false

      - name: Checkout trusted CI control
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: ${{ needs.prepare.outputs.repository }}
          ref: ${{ needs.prepare.outputs.control_ref }}
          token: ${{ secrets.AW_CONTROL_TOKEN }}
          path: ci-control
          fetch-depth: 1
          persist-credentials: false

      - name: Run trusted Windows CI
        env:
          CENTRAL_CI_BASE_SHA: ${{ needs.prepare.outputs.base_sha }}
          CENTRAL_CI_HEAD_SHA: ${{ needs.prepare.outputs.head_sha }}
          CENTRAL_CI_PR_NUMBER: ${{ needs.prepare.outputs.pr_number }}
          TARGET_REPOSITORY: ${{ needs.prepare.outputs.repository }}
          TARGET_PRIVATE: ${{ needs.prepare.outputs.is_private }}
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $script = "$env:GITHUB_WORKSPACE\ci-control\.github\scripts\central-ci.ps1"
          if (-not (Test-Path $script)) {
            throw "Missing trusted central CI script."
          }

          $log = Join-Path $env:RUNNER_TEMP "central-ci-windows.log"
          try {
            & $script -TargetRoot "$env:GITHUB_WORKSPACE\target" *> $log
            if ($LASTEXITCODE -ne 0) {
              throw "Central Windows CI returned exit code $LASTEXITCODE."
            }
          }
          catch {
            if ($env:TARGET_PRIVATE -eq "false" -and (Test-Path $log)) {
              Get-Content $log
            }
            else {
              Write-Error "Central Windows CI failed for $env:TARGET_REPOSITORY. Detailed output is suppressed because the target repository is private."
            }
            exit 1
          }

          Write-Host "Central Windows CI passed for $env:TARGET_REPOSITORY."

  finalize:
    name: Publish Central CI
    needs:
      - prepare
      - linux
      - security
      - windows
    if: always() && needs.prepare.result == 'success' && needs.prepare.outputs.should_run != 'false'
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: write

    steps:
      - name: Publish target status
        env:
          GH_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          REPOSITORY: ${{ needs.prepare.outputs.repository }}
          HEAD_SHA: ${{ needs.prepare.outputs.head_sha }}
          LINUX_RESULT: ${{ needs.linux.result }}
          SECURITY_RESULT: ${{ needs.security.result }}
          WINDOWS_RESULT: ${{ needs.windows.result }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -Eeuo pipefail

          state=failure
          description="Central CI failed"
          if [[ "$LINUX_RESULT" == "success" ]] && [[ "$SECURITY_RESULT" == "success" ]] && [[ "$WINDOWS_RESULT" == "success" || "$WINDOWS_RESULT" == "skipped" ]]; then
            state=success
            description="Central CI passed"
          fi

          gh api --method POST "repos/$REPOSITORY/statuses/$HEAD_SHA" \
            -f "state=$state" \
            -f "context=CI Evidence" \
            -f "description=$description" \
            -f "target_url=$RUN_URL" >/dev/null

          if [[ "$state" != "success" ]]; then
            exit 1
          fi

      - name: Checkout security scan control
        if: ${{ needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: security-control
          persist-credentials: false

      - name: Setup Node for security scan control
        if: ${{ needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24

      - name: Dispatch central security scan
        if: ${{ needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        env:
          AW_REPOSITORY_POLICY: ${{ vars.AW_REPOSITORY_POLICY }}
          AW_CONTROL_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          AW_INGRESS_TOKEN: ${{ github.token }}
          AW_CONTROL_REPOSITORY: ${{ github.repository }}
        run: |
          node security-control/scripts/dispatch-security-scan.ts \
            "${{ needs.prepare.outputs.repository }}" \
            "${{ needs.prepare.outputs.head_sha }}" \
            "${{ needs.prepare.outputs.pr_number }}"

      - name: Checkout deploy control
        if: ${{ needs.prepare.outputs.pr_number == '0' && needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Node for deploy control
        if: ${{ needs.prepare.outputs.pr_number == '0' && needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24

      - name: Attest deploy source main write
        if: ${{ needs.prepare.outputs.pr_number == '0' && needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        env:
          AW_CONTROL_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          AW_REPOSITORY_POLICY: ${{ vars.AW_REPOSITORY_POLICY }}
          MAIN_WRITE_AUDIT_REPOSITORY: ${{ needs.prepare.outputs.repository }}
          MAIN_WRITE_AUDIT_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: node scripts/audit-main-writes.ts

      - name: Dispatch automatic deploy
        if: ${{ needs.prepare.outputs.pr_number == '0' && needs.linux.result == 'success' && needs.security.result == 'success' && (needs.windows.result == 'success' || needs.windows.result == 'skipped') }}
        env:
          GH_TOKEN: ${{ github.token }}
          AW_CONTROL_TOKEN: ${{ secrets.AW_CONTROL_TOKEN }}
          AW_REPOSITORY_POLICY: ${{ vars.AW_REPOSITORY_POLICY }}
        run: |
          node scripts/dispatch-central-deploy.ts \
            "${{ needs.prepare.outputs.repository }}" \
            "${{ needs.prepare.outputs.head_sha }}" \
            "${{ needs.prepare.outputs.pr_number }}" \
            policies/deploy.json \
            policies/runner.json
